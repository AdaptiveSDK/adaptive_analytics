name: adaptive_analytics_example
description: Demonstrates usage of the adaptive_analytics plugin.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  adaptive_core:
    path: ../../adaptive_core
  adaptive_analytics:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
